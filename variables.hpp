#ifndef VARIABLES_HPP
#define VARIABLES_HPP
	//globals to avoid magic numbers when referring to buy/sell flags in the rest of the code
	const int BUY = 0; 
	const int SELL = 1;
	const int FAIL = -200;
	const int NO_MATCH = -100;
#endif 